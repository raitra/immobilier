        {% extends 'base.html.twig' %}
        {% block title %}Insert Commune {% endblock %}

        {% block body %}            
            
            <div class="container">
            {# {% apply upper %} #}
                <h1 class="mt-3" style="color:#8a9e00;">Insert Commune</h1>
            {# {% endapply %}             #}
                <div class="form">
                    <div class="form-group">
                        {{form_start(form)}}
                            {{form_widget(form)}}
                        {{form_end(form)}}
                    </div>
                </div>
            </div>
        {% endblock %}
    