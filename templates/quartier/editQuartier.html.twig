{% extends 'base.html.twig' %}
{% block title %}
    Modifier Quartier    
{% endblock %}

{% block body %}
    <div class="container">
        {% apply upper %}
            Modification d'un cartier
        {% endapply %}
        <div class="form">
            <div class="form-group">
                {{form_start(form)}}
                    {{form_widget(form)}}
                {{form_end(form)}}
            </div>
        </div>
    </div>
    
{% endblock %}