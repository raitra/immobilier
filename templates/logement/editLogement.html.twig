{% extends 'base.html.twig' %}
{% block title %}
    Modifier Logement    
{% endblock %}

{% block body %}
    <div class="container">
        {% apply upper %}
            Modifier un Logement
        {% endapply %}
        <div class="form">
            <div class="form-group">
                {{form_start(form)}}
                    {{form_widget(form)}}
                {{form_end(form)}}
            </div>
        </div>
    </div>
    
{% endblock %}